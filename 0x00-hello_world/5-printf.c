#include <stdio.h>

/**
 *main - Prints characters with printf and is the entry point of code
 * Return: 0 if affirmative
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
